import { Button } from '@components';
import { Wrap, Form } from './style';

const LoginForm = () => {
  return (
    <Wrap>
      <Form>
        <Button>로그인</Button>
      </Form>
    </Wrap>
  );
};

export default LoginForm;
